export type PlayerPosition = { x:number, y:number };

export type PlayerSkin = { color: string, image?: string};

export type TableBorders = { top:number, right:number, bottom:number, left:number }

export type PlayerSpeed = number;

export type PlayerSize = { height: number, width:number }

export type PlayerInfo = {

}