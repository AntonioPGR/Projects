import styled from "styled-components";

const HeaderContainer = styled.header`
  padding: 10px;
  text-align: center;
`

const TitleContainer = styled.div`
  margin-bottom: 10px;
`

const Title = styled.h1`
  font-size: 2rem;
`

const SubTitle = styled.h2`
  font-size: 1.2rem;
`

export { HeaderContainer, TitleContainer, Title, SubTitle }